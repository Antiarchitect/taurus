.pair{:id => "#{pair.id}"}
  .pair_content
    = "#{pair.id}"
    %br
    %abbr{:title => "#{pair.discipline}"}
      = "#{pair.discipline}"
    %br
    %abbr{:title => "#{pair.lesson_type}"}
      - short_lesson_type = truncate("#{pair.lesson_type}", :length => 6, :omission => '.')
      = short_lesson_type
    %br
    %abbr{:title => "#{pair.full_lecturer}"}
      = "#{pair.lecturer}"
    %br
    = "#{pair.groups}"
  .management_panel
    =# link_to image_tag('show_12.png'), pair_path(pair.id), :method => :get, :class => 'lbOn'
    = link_to image_tag('edit_12.png'), edit_pair_path(pair.id), :class => 'lbOn'
    = link_to_remote image_tag('delete_12.png'), :url => pair_path(pair.id), :method => :delete