jQuery(document).ready(function($){
  <% if @pair %>
    $('#<%= escape_javascript @container %>').prepend('<%= escape_javascript render :partial => @pair %>');
    $('#<%= @pair.id %>').draggable();
    $.get('/editor/pairs/' + '<%= @pair.id %>' + '/edit', null, "script");
  <% end %>
});